import React, { Component } from 'react';

import {
  StyleSheet,
  View,
  Text,
} from 'react-native';
import {Left_Menu} from './Route';
export default class App extends Component {
  render() {
    return (
      		<Left_Menu/>
    );
  }
}
const styles = StyleSheet.create({
});
